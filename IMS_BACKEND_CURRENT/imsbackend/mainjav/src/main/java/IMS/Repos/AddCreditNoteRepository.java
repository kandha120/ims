package IMS.Repos;

import IMS.Entity.AddCreditNote;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AddCreditNoteRepository extends JpaRepository<AddCreditNote, Long> {
}
